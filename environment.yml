name: axiom_repair
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy>=1.24
  - pyyaml>=6.0
  - tqdm>=4.66
  - pip:
      - ontology-transformer>=0.1.6
      - rdflib>=7.0
      - torch>=2.1.0
      - sentence-transformers>=3.0
      - safetensors>=0.4.3
      - tokenizers>=0.21,<0.22
      - transformers>=4.43.0
      - accelerate>=0.27.0
      - bitsandbytes>=0.43.0
      - openai>=1.30.0
